USDJPY:
  sec_type: "CASH"
  ibkr_symbol: "USD"          # CASH convention: symbol = base currency
  currency: "JPY"              # currency = quote currency
  exchange: "IDEALPRO"
  what_to_show: "MIDPOINT"     # forex has no consolidated trade tape
  use_regular_trading_hours: false
  tick_size: 0.001
  asset_class: "fx"
  expected_session: "24/5"

ETHUSD:
  sec_type: "CRYPTO"
  ibkr_symbol: "ETH"
  currency: "USD"
  exchange: "PAXOS"
  what_to_show: "AGGTRADES"
  use_regular_trading_hours: false
  tick_size: 0.001
  asset_class: "crypto"
  expected_session: "24/7"

XAUUSD:
  sec_type: "CMDTY"
  ibkr_symbol: "XAUUSD"
  currency: "USD"
  exchange: "SMART"
  what_to_show: "MIDPOINT"     # spot metals have no consolidated trade tape either
  use_regular_trading_hours: false
  tick_size: 0.001
  asset_class: "commodity"
  expected_session: "24/5"

# DISABLED: confirmed blocked, not just unconfirmed. Standalone testing showed
# both SPXUSD (IND) and its SPY (STK) fallback fail identically with IBKR
# error 2188 ("Up-to-the-second historical data requires additional
# subscription for the API") -- this account has no US equities/index
# market-data subscription at all, so swapping tickers won't help. XAUUSD
# (commodity) fetches fine, confirming this is subscription-specific, not a
# broader connectivity issue. Re-enable once a US market-data subscription is
# added to the IBKR account.
# SPXUSD:
#   sec_type: "IND"
#   ibkr_symbol: "SPX"
#   currency: "USD"
#   exchange: "CBOE"
#   what_to_show: "TRADES"
#   use_regular_trading_hours: true
#   tick_size: 0.01
#   asset_class: "equity_index"
#   expected_session: "broker_defined"

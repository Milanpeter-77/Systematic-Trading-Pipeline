# ---------------------------------------------------------------------------
# Manually curated list of validated strategy candidates approved to run
# live in the execution package.
#
# This file is edited BY HAND, never generated. A candidate only belongs
# here after a human has reviewed it in results/gate/final_survivors.csv
# and decided to promote it. There is no automatic promotion step (see
# src/execution/strategy_loader.py).
#
# Each entry must be a candidate_id string that appears verbatim in the
# `candidate_id` column of results/gate/final_survivors.csv.
#
# Starts empty -- uncomment / add entries only once you have actually
# reviewed and approved a candidate for live trading.
# ---------------------------------------------------------------------------

deployed_strategies: []
  # - "trend__XAUUSD__fast_window-12__slow_window-120"
  # - "mean_reversion__SPXUSD__entry_z-2__lookback-48"
